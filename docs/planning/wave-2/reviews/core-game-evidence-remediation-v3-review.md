# W2-REV-04 — Fresh aggregate review of W2-GAME-EV-REM-02

**Issue:** #219  
**Review mode:** `DEGRADED_SINGLE_AGENT_FRESH_EPISODE`, with actor/session distinct from Issue #217 producer and Issue #212 prior reviewer episodes.  
**Claim:** `5284241534`  
**Base:** `main@ae945e998ff95b9bf05041f767c5eb8775502031`  
**Reviewed Issue #217:** terminal status `5284207480`, head `ac03b002fa8ce7237d5f9236d1cbcc1891d0124d`, substantive work `a1073035f239ba501167f7f8150c05794ce1cc36`, draft PR #218.  
**Disposition:** **CHANGES_REQUIRED — 0 BLOCKER / 1 MAJOR / 1 correction-requiring MINOR**.

This review cold-read the exact frozen Issue #217 packet and independently recomputed its mechanical claims before reconciling producer dispositions. Issue #217 and its branch are immutable review inputs.

## Exact reviewed identities

- policy/search blob `fa02ab4c13b247bc2df954db8b0c9ef74a9e84d9`;
- progression/transition blob `39d5deab192cf49a41566e5cfc70f5a658296b22`;
- automation-decisions blob `50f64d428a5261e9b319acb51b30ccbce0c34a39`;
- normalized v3 results blob `b901850128c0aaad84f3ddb5679108a13dc60088`;
- producer dispositions blob `cc81441aa34cdd535ee92a3b16b2ee8dcd62d0af`;
- report blob `7cd4ed922048b1854c9666b27fc9a7cd71609ebe`.

The v3 result object binds those three machine-evidence blobs exactly. Its unaffected set names `GDF-E1`, `GDF-E3`, `EPA-E1`, `EPA-E2`, `EPA-E4`, `EPA-E5` and points back to immutable v2 result blob `c57be3ef32cb2b915aa736d4c007e671e42680b6` with disposition `UNCHANGED_NOT_RERUN_NOT_UPGRADED`; no silent status upgrade was found.

## Fresh mechanical replay — clean portions

### Policy/search and behavior mechanisms

Using only the retained initial state, action order, numeric effects, and nonnegative-stock rule, complete breadth expansion reproduces the exact feasible frontier counts:

- depth 1: **7**;
- depth 2: **49**;
- depth 3: **339**;
- depth 4: **2327**.

At depth 4, the declared objective formulas and lexicographic tie-break reproduce every retained winner exactly:

- wealth → `trade, trade, trade, trade`, score `26.0`;
- self-sufficiency → `forage, forage, forage, forage`, score `26`;
- mastery → `craft, craft, learn, learn`, score `20`;
- social-capital → `service, service, service, service`, score `20`;
- joint-balanced → `forage, service, service, service`, score `2.0`.

The declared cross-objective opportunity-cost tuples also reproduce exactly: gaps `32`, `20`, `20`, `20`, `19`. Five distinct traces win the five objectives and no single trace wins them all. The optimizer therefore searches generated cross-system action sequences rather than selecting among the four fixed v2 lifestyle schedules.

Independent state replay also reproduces all four versioned policy traces and terminal states. Scripted, bounded-rational/noisy, optimizer, and seeded fuzz are behaviorally distinct deterministic mechanisms on one shared state/action model. This closes the substance of `W2-REV3-M01` for `GDF-E2`, `EPA-E3`, and `AGE-E3`.

### Progression mutation validation

The closed `requires_all` graph is sufficient to reproduce the base reachability and all three exact mutations:

- base: all five required goals reachable, no unknown symbols, no cycle;
- `missing_requirement`: unknown `RESOURCE_GHOST`, `trader_goal` unreachable;
- `cycle_injection`: deterministic cycle `crafter_goal -> trader_goal -> crafter_goal`, both goals unreachable;
- `path_loss`: unknown `RESOURCE_GHOST`, `mature_goal` unreachable.

The progression half of `W2-REV3-M02` is therefore mechanically reconstructable and supports the bounded `EPA-E7` rerun.

### Transition frontier count only

If the producer-intended transition semantics are supplied externally, breadth expansion in stable action order reproduces frontier counts **4 / 16 / 63** and the retained witness terminal states for `free_step,free_step`, `advance_stage`, `buy_unlock,reset`, and `market_trade`. That is useful evidence, but it does not cure the closed-semantics defect below.

### Automation tier surfaces

The exact surface identities reproduce tier memberships `1 / 3 / 5`; `partial` is a strict superset of `manual`, and `strong` is a strict superset of `partial`. Every surface has two named options with distinct option records, and the valid Issue #210 payback winner counts `1 / 3 / 5` are explicitly retained rather than recomputed or upgraded.

## Findings

### W2-REV4-M01 — AGE-E4 exploit findings are still not mechanically generated by a closed transition/check schema

**Severity:** MAJOR  
**Affects claimed PASS:** `AGE-E4`  
**Reopens:** the exploit-search half of `W2-REV3-M02`.

The v3 packet materially improves search-frontier retention, but the exact transition object still requires reviewer-invented semantics before its classifications can be derived:

1. action `requires` are raw strings such as `coin>=4`, `unlock=false`, `setup_spent>0`, and `market_opportunity>0`; no versioned expression grammar or evaluator semantics are retained;
2. action `effects` mix numeric values, booleans, `"+setup_spent"`, and `"unchanged"` without a closed update rule. For example, `advance_stage: {stage: 1}` is not mechanically forced to mean increment rather than assignment by the object itself;
3. `checks` are English sentences rather than typed predicates or executable validator bytes;
4. `first_findings` already contain the witness traces and classifications, while the retained enumeration output is only aggregate frontier counts plus those supplied first-finding rows.

A fresh reviewer can reproduce `4 / 16 / 63` by adopting the obvious intended interpretation, but cannot prove from the retained machine object alone that the search *generated* `VIOLATION` versus `ALLOWED` classifications. The packet therefore still proves classification of supplied witnesses more strongly than mechanically generated exploit discovery. This is the exact authority gap Issue #217 was required to close: outcomes must derive from exact input objects rather than prose or supplied witness classifications.

**Required correction:** publish one closed, versioned transition/update/precondition/check schema or deterministic executable validator plus exact retained search output. The validator must derive feasible children and violation classes from exact inputs, retain deterministic first-hit/search evidence, and make the current `AGE-E4` result a consequence of those bytes rather than natural-language interpretation. Preserve the current frontier and historical lineage; rerun only `AGE-E4` unless the correction changes another affected predicate.

### W2-REV4-m01 — automation object asserts a false all-options-change invariant

**Severity:** correction-requiring MINOR  
**Affects:** internal mechanical consistency of `GDF-E4` evidence, but does not independently reopen the semantic tier-surface correction.

`mechanical_result.every_option_changes_at_least_one_fictional_state_field` is `true`, but `choice_surfaces.expansion.hold` is exactly `{coin: 0, capacity: 0}`. That option changes no declared fictional state field. The stronger boolean assertion is therefore false under the packet's own data.

The core `W2-REV3-M03` correction remains materially improved: named categories exist, higher tiers are strict supersets, option records are distinct, and higher tiers expose modeled configuration/logistics/expansion/quality choices rather than anonymous counts. A no-op `hold` can be a legitimate modeled choice, so this mismatch is bounded rather than MAJOR. The packet must nevertheless either correct the invariant or change the option semantics/data so the recorded boolean is mechanically true.

## Result disposition

The following affected v3 reruns are independently supported in this bounded review: `GDF-E2`, `GDF-E4` subject to the minor record correction above, `EPA-E3`, `EPA-E7`, and `AGE-E3`.

`AGE-E4` is **not admissible as PASS** because the retained object does not mechanically derive exploit classifications under a closed transition/check semantics. Consequently the producer summary `6 PASS / 0 FAIL / 0 INCONCLUSIVE / 0 NOT_RUN` is not acceptable as a fully reviewed six-PASS packet.

Overall disposition is **CHANGES_REQUIRED**. Exactly one bounded remediation successor should close `W2-REV4-M01` and `W2-REV4-m01`, without reopening clean unaffected evidence or spawning another broad evidence rewrite.

## Authority and blocker state

`IR-BLOCKER-GAME-EVIDENCE` remains **OPEN** for `SCOPE-CORE-GAMEPLAY-v1`. Human fun/player preference remains out of scope. This review grants no gameplay or production implementation, engine selection, release approval, implementation readiness, verification PASS, legal/provider authority, integration authority beyond separately applicable owner directives, or canonical status. Any main integration remains separately authorized and squash-only.
